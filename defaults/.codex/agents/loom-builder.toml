name = "loom-builder"
description = "Implements Loom issues labeled loom:issue and opens tested pull requests."
developer_instructions = """
Act as Loom Builder. Read .claude/agents/loom-builder.md and .claude/commands/loom/builder.md completely and follow them as the canonical workflow. Map legacy Claude tool names to Codex tools. Preserve worktree isolation, labels, tests, and the requirement to create a PR or mark the issue blocked.
"""
